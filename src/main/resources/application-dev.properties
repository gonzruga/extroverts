server.port=8080

# URL & DATASOURCE
spring.datasource.url=jdbc:mysql://roundhouse.proxy.rlwy.net:41487/railway?createDatabaseIfNotExist=true
#useSSL=false&serverTimezone=UTC
#useSSL=false: Disables SSL encryption for the connection.
#serverTimezone=UTC: Sets the server's time zone to UTC, which helps avoid time zone mismatches.

spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# CREDENTIALS
spring.datasource.username=root
spring.datasource.password=KnBCBpBScSpeIdJnxCLjYLWIAwzMgLXi

## JPA Hibernate
#spring.jpa.hibernate.ddl-auto=create-drop
spring.jpa.hibernate.ddl-auto=update

## To see queries generated by hibernate (Optional). For search functions
spring.jpa.show-sql = true
spring.jpa.properties.hibernate.format_sql=true

#spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL5Dialect

#spring.jpa.database-platform=org.hibernate.dialect.H2Dialect